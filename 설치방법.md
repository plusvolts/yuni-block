# 윤이 블록 — 설치 방법

1. GitHub 저장소 `plusvolts/yuni-block`에 이 폴더를 올리고 GitHub Pages(main 브랜치, 루트)를 켜요.
2. 갤럭시 탭·폰의 크롬에서 `https://plusvolts.github.io/yuni-block/`을 열어요.
3. 처음 화면 위의 "📲 앱 설치" 버튼(또는 크롬 메뉴 → 홈 화면에 추가)을 눌러 설치해요.
4. 한 번 열고 나면 오프라인에서도 동작해요 (sw.js가 파일을 캐시).
5. 아빠 화면(⚙️, 기본 암호 1234)에서 리딩 캐릭터·아이 이름·시간 제한·별 목표를 정하고, 암호를 새로 바꿔 주세요.

## 새 버전 반영
- `app.js` APP_VERSION과 `sw.js` VERSION을 올리고 push하면, 앱의 아빠 화면 → 🛠️ 백업·업데이트 → "새 버전 확인 → 지금 업데이트"로 받을 수 있어요. 켜면 3초 뒤에 새 버전 알림도 떠요.

## 자동 테스트
```
cd yuni-block
python3 -m http.server 8765
cd test && python3 flow.py tab 1280 800 0 && python3 flow.py phone 390 844 1
```
(playwright + chromium 필요: `pip install playwright && playwright install chromium`)
